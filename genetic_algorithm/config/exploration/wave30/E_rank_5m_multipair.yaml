# ============================================================================
# WAVE30 — E: Multi-Pair Rank 5m (Hierarchical Topology)
# ============================================================================
# Hypothesis: 4 train + 2 val pairs = strongest regularizer (per R5 success)
# 8 islands × 15 pop = 120 individuals, hierarchical topology
# Single 5m TF, 6 pairs total for maximum generalization
# Higher validation weight (0.45 vs usual 0.40)
# Sequential per island (no parallel workers)
# ============================================================================

experiment_name: wave30_E_rank_5m_multipair

genetic_algorithm:
  random_seed: null
  population_size: 15
  generations: 25
  mutation_rate: 0.18
  crossover_rate: 0.7
  elite_size: 3
  tournament_size: 3
  selection_method: 'rank'
  convergence_patience: 15
  adaptive_mutation: true
  max_adaptation_factor: 2.5
  adaptation_step: 0.12
  mutation_cooldown_factor: 0.5
  fitness_sharing: true
  sharing_radius: 0.22
  diversity_threshold: 0.12
  allow_self_crossover: false
  random_immigrants: 2
  mode: 'single_objective'

surrogate:
  enabled: true
  min_training_samples: 80
  filter_percentile: 70
  retrain_interval: 3
  model: "gradient_boosting"
  mutate_skipped: true

generic_island_model:
  enabled: true
  num_islands: 8
  population_per_island: 15
  parallel_islands: false
  specialization:
    rotate_seeds: true
    indicator_pools: true
    indicator_overlap: 0.4
    pair_rotation: false
  migration:
    topology: 'hierarchical'
    interval: 4
    count: 2
    merge_rounds: false

# ── Fitness Weights (balanced with cross-pair emphasis) ───────────────────
fitness_weights:
  profit: 0.12
  sharpe_ratio: 0.16
  sortino_ratio: 0.10
  profit_factor: 0.08
  drawdown: 0.14
  win_rate: 0.12
  trade_frequency: 0.18
  monthly_stability: 0.04
  cross_pair: 0.06

trade_frequency_thresholds:
  auto_scale_by_timeframe: true
  shape: 'gaussian'

fitness_penalties:
  min_trades: 80
  min_trades_per_month: 14
  min_trades_per_month_penalty: 0.05
  max_drawdown: 0.20
  min_win_rate: 0.35
  complexity_weight: 0.01

backtesting:
  timerange: "20210101-20260331"
  stake_amount: 100
  pairs:
    - "BTC/USDT"
    - "ETH/USDT"
    - "SOL/USDT"
    - "BNB/USDT"
    - "XRP/USDT"
    - "PEPE/USDT"
  max_open_trades: 3
  fee: 0.001
  slippage_pct: 0.0005
  exchange: "binance"
  auto_download_data: false
  enable_cache: true
  timeout: 300

pair_validation:
  enabled: true
  training_pairs:
    - "BTC/USDT"
    - "ETH/USDT"
    - "SOL/USDT"
    - "BNB/USDT"
  validation_pairs:
    - "XRP/USDT"
    - "PEPE/USDT"
  weight_train: 0.55
  weight_val: 0.45
  min_val_fitness: 0.0
  validate_top_n_only: 8

walk_forward:
  enabled: false

holdout_validation:
  enabled: false

holdout_monitoring:
  enabled: false

monte_carlo:
  enabled: false

strategy_constraints:
  min_trades: 80
  max_drawdown: 0.20
  min_win_rate: 0.35
  timeframes: ["5m"]
  stoploss_range: [-0.035, -0.008]
  roi_range: [0.003, 0.018]
  max_open_trades_range: [1, 4]
  trailing_stop_positive_range: [0.003, 0.012]
  trailing_stop_offset_addition_range: [0.003, 0.01]

indicators:
  available:
    - 'RSI'
    - 'MACD'
    - 'BBANDS'
    - 'EMA'
    - 'SMA'
    - 'SUPERTREND'
    - 'PSAR'
    - 'CMF'
    - 'VROC'
    - 'MFI'
    - 'ROC'
    - 'TEMA'
    - 'KAMA'
    - 'VWAP'
    - 'STOCH'
    - 'DONCHIAN'
    - 'WILLR'
    - 'AROON'
  min_per_strategy: 2
  max_per_strategy: 4

storage:
  database: "genetic_algorithm/data/wave30_E.db"
  strategy_dir: "user_data/strategies/wave30_E"
  checkpoint_dir: "genetic_algorithm/data/checkpoints_wave30_E"
  checkpoint_interval: 3
  keep_history: true
  min_disk_gb: 5.0
  min_disk_gb_runtime: 2.0
  max_cache_disk_mb: 8000

logging:
  level: "INFO"
  file: "genetic_algorithm/logs/wave30_E.log"
  console: true
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"

visualization:
  enabled: true
  update_interval: 1
  output_dir: "genetic_algorithm/output/exploration/wave30/E_rank_5m_multipair/plots"

terminal_monitor:
  enabled: true
  default_mode: "simple"

hall_of_fame:
  enabled: true
  max_size: 50
  inject_count: 3
  min_fitness: 0.10
  directory: "genetic_algorithm/data/hall_of_fame_wave30_E"

deflated_sharpe:
  enabled: true
