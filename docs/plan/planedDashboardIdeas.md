# Planned Dashboard Ideas – Todo List

## Runs Page (`/runs`)

- [ ] **Hide runs** – Add the ability to hide individual runs from the `127.0.0.1:8501/runs` page without permanently removing them from the database.
- [ ] **Delete runs** – Add the ability to permanently delete individual runs from the database directly from the `/runs` page.

## Database

- [ ] **Evaluate current database system** – Assess whether the existing database solution is sufficient for the features above (hide/delete, filtering, metadata storage).
- [ ] **Implement an advanced database system** – If the current setup is too limited, design and implement a more robust database layer (e.g. with soft-delete support, indexing, and run metadata) to support the planned dashboard features.
